# Relation-policy Qwen canary: one separate synthetic batch

Registered 2026-09-18 before runner implementation or any RPQ provider request.
Preparation baseline: `696f9ca864006f86dcdd083e1e20e6e1d811e85f`.
New protocol identity: `report_evidence_relation_policy_qwen_canary_v1`.
Transport: `report_evidence_relation_policy_qwen_transport_v1`.

## Question, scope and prior observations

Does the frozen explicit relation policy help exact `qwen3.5-plus` distinguish
an unmeasured physical value from a record's explicit no-measurement statement,
while preserving a reported-value positive control? This is a bounded native
compatibility/development test, not unseen accuracy or production admission.

The [RP callback result](results-2026-09-17-claim-relation-policy.md) and
[native wire result](results-2026-09-17-relation-policy-qwen-transport.md) establish
scripted delivery/accounting only. PCQ and CLQ are closed failures; never change
their prompts, labels, artifacts, source identities or occupied output paths.
This protocol does not reopen their unused cases or reuse their allowances.

Before new edits, a socket-blocked replay of the selected RP controls reached
six positional callbacks. Initial bodies measured 4,363 / 4,403 / 4,366 bytes;
final bodies measured 5,141 / 5,181 / 5,071. Each final callback received one
saved-text receipt. These were scripted responses with one fixed fictional
catalog title, not HTTP bytes or model judgments. The earlier 30-snapshot
native MockTransport replay is separately qualified in the wire result.

## Fixed cohort and disclosure

Use the complete original fixture bytes:
`tests/fixtures/report_evidence_relation_policy.json`, SHA-256
`a39622a3f2fd1f40da3d1f112e8d989e78ed25ccef4d369f8b714bde58fadf4c`.
Select exactly RP01, RP02 and RP03, in that order: insufficient, refuted,
supported. RP04-RP08 remain outside this native batch, not fresh held-out data.

These are already examined dependent synthetic development controls with
label-blinded, context-limited LLM reference review, not three independent
samples or human gold. Selection is fixed prospectively; no native-result
selection, reference change, repair or prompt tuning within the batch.

Construct a fresh one-source snapshot per case: report_ref=case_id, source A1,
group academic, title "Fictional relation-policy control", publisher "Synthetic
controls", source_type synthetic_control, origin unknown, accessed_date
2026-09-17. Preserve the literal fixture claim and text. Shared text does not
permit conversation or receipt reuse.

Only the claim, allowlisted catalog, exact policy and actually selected read
enter Qwen. Do not send expected relations, proposition categories, rationales,
scripted answers, historical outcomes, real reports or private RS material.
At most one complete nonblank saved-text read and two requests per case.

## Dedicated implementation and identity

Add only `report_evidence_relation_policy_qwen_canary.py` under
`src/academic_agent/`, root `report_evidence_relation_policy_canary.py`,
dedicated tests and current guidance/results. Use the frozen RP wrapper and
exact RP native adapter/ledger; do not invoke a closed runner, patch globals,
wrap an old adapter, relocate modules or refactor frozen predecessor tests.
Reuse only bounded frozen primitives; explain any necessary limited duplication.

Bind the new runner, CLI, tests, protocol, complete executing project dependency
closure, RP fixture/policy/transport, their protocols and qualification records,
configuration and locked installed dependency versions to an exact committed
SHA. Compare committed blobs and actual fixed-path disk bytes with existing
CRLF-to-LF rules. Reject indirect/reparse paths, hidden source changes and
mismatched package versions. Versions are not installed-package byte attestation.

Default CLI verifies identity only: no dotenv, key read, output creation or HTTP.
Native mode requires a full expected commit, exact fixture hash and new
protocol acknowledgement. The latter records intent, not independent consent
or CI verification. No force, resume, output/case/model override or retries.
Exclusive output is `outputs/report_evidence_relation_policy_qwen_canary_v1`.
Any occupied/partial output blocks a new batch; no deletion/rename workaround.

## Independent mechanical, label and publication gates

Run `run_relation_policy_followup(..., callback=checked_native)` with one
positional request. Revalidate identity before and after each native call,
including errors. Preserve already observed usage when post-response drift
stops execution. First-stage early final, wrong source, partial/offset read or
missing read must stop before another paid request; do not force tool choice.

A mechanical pass requires all frozen inner claim/catalog/core observations,
complete matching native journals and both RP callback audit entries. Check
configured and delivered policy hashes, entry ordinals, canonical callback
hashes/lengths, exact original claim/catalog/system, and complete delivered and
usable receipt IDs. Inner delivery, entry into the native callback, reserved
HTTP intent and received response remain distinct. Never copy one into another.

The final native request must contain the actual full nonblank read, correctly
paired native call and receipt. The final native response must preserve the
strict four-field JSON and unverified semantic flags. Supported/refuted require
the actual receipt as support; insufficient retains the delivered/usable receipt
but no supporting IDs. A valid wrong relation is a mechanical pass and a
separate frozen-label failure, not a corrected answer.

Validate raw integer/boolean types before serialization as well as the exact
serialized publication payload. Reject forged/missing RP audits, changed policy,
receipt/history/body hashes, unknown usage, changed model or incomplete
accounting. The complete HTTP encoding must match the journal and remain within
12,288 bytes after transformations, not merely at callback preflight.

Stop on the first setup, identity, transport, accounting, mechanical, label or
publication failure. Remaining selected cases are explicitly unrun. Overall
batch_passed requires all three mechanical/label/case-publication passes,
known accounting, no pending/stop state and a successfully published summary.
No-review and no-observation are never pass.

Write separate identity/experiment/acknowledgement before dispatch. Preserve
the adapter manifest's live_authorization=False. Reserve intent before sending;
write, flush, fsync and close before write-once case/summary publication. Pending
files cannot signal completion. Failed publication cannot buy another case.
This local single-owner journal is not distributed admission, provider
exactly-once, protection from hostile deletion or disk-loss recovery.

## Bounds and authority

The equivalent standing synthetic scope is six sequential exact-model requests,
USD 0.10 soft stop, with one in-flight small overshoot accepted. The new identity
is not unused historical authority. The parent may dispatch only after this
runner is committed, reviewed and all exact-SHA CI jobs pass; this implementation
stage makes no provider call or real-key read.

Use only process DASHSCOPE_API_KEY at the pinned Beijing endpoint
`https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`.
No dotenv/alternate keys in the runner. Any later parent-side load must select
only that assignment without printing or persisting it. Non-thinking exact
qwen3.5-plus, non-streaming, temperature zero, no parallel tool calls,
512 output tokens, 64 KiB response, 10-second connect/60-second request bounds,
TLS verification, trust_env=False, no proxy, redirect, retry, search, fallback,
repair, recovery, provider replacement or production route.

Retain the frozen conservative rates/reservations: USD 0.573/3.44 per million
input/output tokens, 16,384 input-token reservation, USD 0.011149312 per request
and USD 0.066895872 for six. These are inherited estimates, not current prices
or invoices. Unknown usage retains its reservation and stops. Return safe
categories only; refuse secret echoes before persistence.

## Verification and later judgment

Use fake credentials and real RP, frozen local tools, exact new adapter/ledger
and intercepted HTTP, not simulated adapter delivery. Prefer focused
parameterized failure seams to duplicating all frozen transport scenarios.

Cover a full six-request batch, exact full read and policy delivery, hidden
labels, early final/partial read, wrong-but-valid relation, tampered audit and
receipt, raw counter types, protocol/model/unknown-usage failure, pre/post-call
identity drift, occupied output, and setup/case/summary persistence failure.
Exercise actual CLI identity-only isolation and invalid arguments without
reading ambient credentials. Process-global guards must end with the operation,
before pytest reporting.

Reinject omissions in RP callback-audit admission, first-stage complete-read
admission and label-stop logic. Provide valid downstream responses so failures
assert observed dispatch boundaries, not missing mocks. Restore exact bytes
and rerun focused tests. Preserve frozen file hashes; run full baseline/final,
latest Ruff and narrow Pylint. Require a different read-only strong reviewer.
Do not weaken assertions, ignore warnings or add skips.

A later single native batch receives a fresh LLM judgment, with only the claim,
actually delivered text/receipt, candidate answer and neutral rubric initially.
Hide labels, target outcome and past reviews; record inherited-context limits,
requested role/model, unavailable effective metadata and quotation matches.
First judge the proposition relation, then answer support; missing answers are
not_reviewable. This is fallible LLM review, not human gold. No extra paid
judging call is part of this protocol.

Close the native batch whether pass or fail, preserving exact artifacts and
separate mechanical/reference/judgment outcomes. Only synthetic aggregates and
method limits may be public; raw reviews stay local/ignored. No automatic
retuning, repeat batch, private-data transfer, production enablement or PR merge
follows from a successful engineering or native result.
