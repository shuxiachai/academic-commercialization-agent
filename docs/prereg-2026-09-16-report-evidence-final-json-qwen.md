# Final-only JSON saved-evidence Qwen canary

Registered: 2026-09-16, before implementation and any JQ provider request.
Baseline: `951be96d3f53145f4004cdc55d2206a0ed96a0bc` (PR #148).
Protocol identity: `report_evidence_final_json_qwen_canary_v1`.
Transport identity: `report_evidence_final_json_qwen_transport_v1`.

## Question and authority

SQ01 delivered its saved excerpt but failed strict final-envelope parsing after
prose plus fenced JSON. This candidate requests JSON Object output only in the
final-only stage, without changing the core parser, tool limits or old batch.
SQ01 stays failed and SQ02 unrun; FQ and v8 conclusions also remain unchanged.

Implementation and regression start offline. The user's standing instruction
and this continuation authorize one new, same-scope synthetic validation after
independent review, identity freeze and tests, without another confirmation:
at most six sequential exact `qwen3.5-plus` requests, USD 0.10 total soft stop,
accepting a small in-flight overshoot. This is a separate allowance, not the
unused SQ/FQ balance. Only the parent operator executes live requests. Bind the
final implementation commit, this fixture hash and configuration before dispatch;
the preregistration commit alone is insufficient. CLI acknowledgement records
operator intent, not independent verification of user consent.

No customer reports, benchmark text, reserved cohorts, external search, retries,
redirects, repair, fallback, recovery, model replacement or production activation.
Any first-case failure closes the batch; no later case or automatic new batch.
More money, real user material or production admission is outside this scope.

## New synthetic controls

Fixture: `tests/fixtures/report_evidence_final_json_qwen_canary.json`.
Raw-byte SHA-256:
`f0e8233a7b0e0d86783f069d96880ace941cd0ac16dbfa934e723d653febb5ca`.
The nine-field flat list fixes JQ01 then JQ02, exact questions and record text,
publisher `Synthetic example`, date `2026-09-16`, and no DOI or external URL.
Expected-answer labels are not sent to the model. These are developer-authored
synthetic controls, not independent unseen retrieval or human-effectiveness data.

| Case | Saved material | Required observation |
|---|---|---|
| JQ01 | A9, academic/abstract, synthetic piezoelectric vibration sensor; laboratory resonance 18 Hz, no field deployment or lifetime testing | Actual complete saved-text read, paired delivery in a later accepted request, accepted `answered_with_evidence` citing the issued ID |
| JQ02 | M6, market/unknown, synthetic river membrane business; text absent | Actual native read, paired `missing_text` delivery in a later accepted request, accepted `abstained` with no evidence IDs or successful-read receipts |

Direct reads are allowed but are not lookup observations. Early abstention,
no-tool answers, partial reads and lookup-only output do not pass JQ01. Merely
saying that text is missing without reading does not pass JQ02. The positive
answer's 18 Hz and testing-limit statement receive a separate narrow parent
content inspection; this is not an automated/general entailment score.

## New wire contract, frozen dependencies

Add separate transport, ledger, runner and CLI modules; preserve all existing
source/fixture/test/protocol/result bytes. Reuse stable frozen `_post`, encoding,
usage, secret and stage-admission helpers, not a fixed SQ runner or its globals.
No runtime monkeypatching, private globals replacement, caller-controlled
manifest or after-reservation body rewrite is an implementation strategy.

Only final-only HTTP adds `response_format={"type":"json_object"}`; it omits
`tools`, retains `tool_choice="none"` and remains non-thinking. Initial/read
requests do not send `response_format`; native call IDs, paired results and the
entire history are retained. Final-only requires a standalone ASCII `JSON` word
(case-insensitive) in a string system/user content before reservation. Tool or
assistant content cannot satisfy this guard. Do not repair missing instructions.
The existing code-owned system instruction already meets the prerequisite.

The [official structured-output documentation](https://help.aliyun.com/zh/model-studio/qwen-structured-output)
lists non-thinking Qwen3.5-Plus for JSON Object output and its JSON-keyword
requirement. JSON Object does not guarantee a schema. Its compatibility with
this exact native-tool conversation is a hypothesis, not an already observed
fact. Keep strict whole-response JSON, envelope/schema and evidence-ID checking;
do not strip Markdown, extract a JSON substring or repair a rejected answer.

Construct the complete body before serialization and reservation. Journal hash
and actual HTTP bytes must agree, including `response_format`. Use a new,
code-owned manifest with its own configuration identity, retaining frozen ledger
reserve/finish semantics. Imports and default CLI mode perform no key lookup,
output creation or provider call. Bind new modules/tests/protocol/fixture and
all executing dependencies to committed blobs plus actual disk hashes; compare
installed dependency versions with the lock. A status-hidden edit must fail.

Use only process `DASHSCOPE_API_KEY`, the frozen official Beijing endpoint and
exact model; never dotenv/fallback automatically, change global provider config
or print credential/provider exception bodies. The authorized parent may load
only that dedicated assignment into its invocation process, restore the prior
value afterwards and leave the environment file untouched. Persist identity,
authorization and manifest before dispatch in an exclusive fresh output path.
No occupied-directory reuse, summary overwrite or resume. Identity must remain
valid before and after each request; already observed usage survives drift.

## Budget and stop rules

Retain two tool attempts and three transport turns per case, one tool per turn,
512 output tokens, 12 KiB request/64 KiB response, 10-second connect and 60-second
owned request timeout, non-streaming, temperature zero, TLS verification and no
environment proxy, retry or redirect. One ledger owns at most six requests.
Frozen rates are USD 0.573 input / 3.44 output per million, with 16,384 input
reservation and USD 0.011149312 reserved per possible request. Verify these are
still conservative before live use. They are an estimate, not an invoice.

Transport/model/usage rejection, core or policy failure, missing actual paired
delivery, unknown usage, unresolved dispatch, identity drift or persistence
failure stops all later requests. Preserve received usage even when an output
fails. Successful transport alone is not a case pass. Write-once results and
summary distinguish failed, passed, unrun and unpersisted states. Two passed,
accounted cases are required for the mechanical batch gate. Semantic support
remains `not_assessed`, answer verification `not_verified`; neither a two-case
pass nor a correct-looking failed response proves report utility or production
readiness. This is not shared paid admission or provider exactly-once delivery.

## Offline and live acceptance

Before/after full zero-provider suite, focused tests, latest Ruff, narrow Pylint,
lock check and separate read-only review. Observe policy -> actual local tools ->
intercepted HTTP -> journal -> result, not a configuration field alone. Cover
all stages, exact paired IDs/text, rejected declarations, JSON-keyword checks,
invalid final envelopes and unknown usage preserving cost; test default identity
before keys/outputs, source drift, setup/final persistence failure and first-case
failure preventing the next HTTP. Preserve Windows socketpairs; intercept HTTP
rather than block the interpreter's internal sockets. No weak assertions/skips.

Re-inject removal of the final JSON wire member, require a seam assertion to
fail, restore the source hash and rerun. Do not mutate a historical frozen module.
After committed identity verification, the parent may execute the single bounded
batch above in a new directory, publish qualified aggregates and hashes, and
keep raw artifacts local. Any failure is retained without automatic repairs.
Production integration remains explicitly out of scope even if both cases pass.
