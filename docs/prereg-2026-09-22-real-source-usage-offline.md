# RU: real saved-source selection and usage, offline preparation

Registered on 2026-09-22, from `ba45c38b00095de1b73a459c990a31f59b15085e`,
before observing the new questions through the keyword baseline or a provider.
Protocol identity: `saved_source_real_usage_offline_v1`.
This is local preparation and intercepted engineering rehearsal, not a live
authorization, native accuracy result or production activation.

## Question and existing evidence

The closed RQ batch verified two fictional native selection/receipt journeys.
The later usage-delivery successor added operation-specific client accounting,
but its new path has only intercepted HTTP evidence. Prepare a small real-data
packet to test that next seam without confusing it with historical RQ or RS.
All previous batches, fixtures, references, outputs and reported failures remain
unchanged and closed in their original roles.

Before source-title inspection, select the first saved repetitions of two
different existing benchmark topics, food-production materials and flexible
electronics. Read-only inventory found 30 local benchmark directories: 29
marked success/live and one success/fixture. Both selected reports are marked
success/live, with exact topic agreement between metadata and source collection.
These are current-disk association checks, not attestation of original runs.

Preserve the complete original source order and field values: D1 has 19 entries
(5 academic / 8 patent / 6 market); D2 has 23 (8 / 8 / 7). Their catalogs measure
2,641 and 3,293 ASCII bytes. All 42 saved text values are nonempty and at most
1,500 code points. A saved value of length 1,500 may already be truncated; this
does not establish a complete abstract or full paper. Do not remove distractors,
rewrite text, change dates or perform retrieval to improve these conditions.

The baseline whole suite passed 6,677 tests and 1,592 subtests before edits.
Historical benchmark evidence is consumed material. Newly written questions
do not turn it into unseen data, independent labels or measured user utility.

## Fixed tasks and honest reference state

Freeze RU01-RU04 in that order, with RU01/RU02 attached to D1 and RU03/RU04
attached to D2. Questions contain no source IDs, offsets or expected states.

| Case | Intended title-location task | Reference handling |
|---|---|---|
| RU01 | A bioprocess-engineering study | One candidate draft |
| RU02 | A broad scaffolding-review request | Set-valued draft; do not force a unique answer |
| RU03 | Printed interconnects and vias | One candidate draft |
| RU04 | A clearly different research area | Empty candidate set means no matching catalog title, not no literature anywhere |

Exact questions, author-assisted keywords, proposed reference sets and separate
scripted choices stay in the private packet. References are AI-authored drafts,
not human gold; independent blind review remains `not_run` in this preparation.
Prepare its view with only questions and complete visible ID/title catalogs,
excluding proposed labels, scripts, keywords, saved text and baseline outcomes.
If later judging occurs, retain actual provenance and uncertainty under the
[LLM-only policy](llm-review-policy.md), without fabricating a completed review.

Scripted selections are independently specified controls, not inferred from
reference labels. Changing labels must invalidate the old binding; deliberately
rebinding changed labels must not change request bytes or scripted execution.
Any comparison against these drafts is explicitly provisional, never accuracy.

## Private packet and data boundary

Bind original report, metadata and source bytes, the full projected snapshots,
catalogs, ordered questions, assisted keywords, configuration, scripts and
independent references/provenance. Reuse the current saved-byte projection; do
not construct a second date-normalizing or relevance-filtering pipeline.

Derive an explicit sandbox-only run alias from the source-byte hash under a
fixed timestamp prefix. It is only for temporary local owner/receipt fixtures,
not the original run identity or a production read capability. Original-byte
hashes remain separate from the derived snapshot/catalog identities.

Raw files, packet, labels, source windows, preview bodies and detailed rehearsal
transcripts remain under a new ignored local directory. Public tests use only
synthetic data. Do not publish private run links, original paths, question text,
per-source labels or raw reports. A hash is an identity check, not permission to
publish or transmit its source material.

Keep `live_authorization=false` and `native_efficacy=not_run`. This change has no
real key discovery, endpoint override, caller-supplied callback, live flag or
paid runner. The earlier one-off credential and batch grants are not reused.
A future provider request needs its own exact implementation/data boundary and
applicable budget authority, covering every visible title rather than only the
reference answer. Its intended dynamic input is question plus all visible
ID/title entries; saved text, report prose, metadata, labels and credentials
must not be prompt content.

## Two independent offline observations

1. Run the unchanged Sources Node observer on grouped ID/title-only records.
   For each question, measure both the literal whole-question input and the
   previously frozen assisted keywords. Record candidates, provisional reference
   intersection, ambiguity and unavailable observation explicitly. This omits
   saved summaries and publishers; it is not the full production Sources search
   surface or a measured user-query/time-saving study. A long-question literal
   miss is not evidence of a model advantage.
2. In a dedicated subprocess, install internal fake-key HTTP interception and
   external-network denial before constructing a selector. Run four ordered
   scripted selections through the current accounted Qwen wrapper, usage app,
   controller/store, POST and GET. Compare each complete intercepted HTTP body
   byte-for-byte with its preview, not merely a matching source ID or a callback
   counter. This is a real adapter path with a scripted response, not native
   model inference. No external selector or network option is accepted.

The input snapshot and labels never authorize dispatch. Preserve current
complete-catalog requirements, title/callback/HTTP byte limits and one-read
scope; reject omission or excess rather than increasing limits. Original files
must remain unchanged after preparation and rehearsal. Occupied packet outputs
are not overwritten or silently resumed.

POST and GET must preserve the full versioned envelope, exact saved result and
accounting projection. Original execution and delivery-read counters remain
different facts. GET must not add another model entry or paid admission. Fake
response tokens and estimated amounts are explicitly scripted; unknown remains
unknown and is not zero. A failed receipt/wire/identity check stops later scripted
cases and preserves the unrun state. A missing baseline observation is not an
empty candidate set or automatic pass.

## Acceptance and remaining gate

Use focused synthetic regressions for raw-byte drift, malformed source/metadata,
complete-catalog capacity, set-valued labels and label-free input construction,
script independence, before/after asset identity, HTTP guard failure, actual wire
comparison, full POST/GET delivery and no redispatch. Reinject key defects and
require unchanged assertions to fail before restoring exact source hashes.
Run the complete offline suite, current Ruff and narrow Pylint, then obtain an
independent read-only review. Process-level interception is not a hard sandbox
against malicious trusted Python or provider-level exactly-once execution.

Completion means a frozen private development packet, a prepared blind-review
view and separately qualified offline observations. It is not completed blind
judging, a paid-model test, a new semantic-support flag or permission to expose
the Tool Calling feature publicly.
