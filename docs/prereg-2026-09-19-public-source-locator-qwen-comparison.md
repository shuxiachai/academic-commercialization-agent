# SLCQ: native selection against the frozen public-title keyword baseline

Registered before implementation or native observation on 2026-09-19 from
`6ac8f426ff54bfcef8b9a420a47827332985dd30`. New protocol identity:
`report_evidence_source_locator_comparison_qwen_v1`.
This is a separate bounded development comparison, not a production route.
Closed SLQ and all earlier answer/relation batches remain closed.

## Fixed question, public input and fair comparison

Use the exact six ordered questions, twenty ID/title entries and acceptable
sets in [the SLC fixture](../tests/fixtures/report_evidence_source_locator_comparison.json).
Its SHA-256 is `74bc2a26041a9365802d635e7e348387a47ce48404718793a2e5ce2f78c0e867`.
Do not duplicate or edit the fixture, keywords, references, original public
battery report, existing Sources renderer, observer or earlier results.

The [observed SLC baseline](results-2026-09-19-public-source-search-baseline.md)
has five positive-reference cases covered by prepared keywords: four unique,
one valid P5/P8 pair; its one no-fit control returns no titles. The unchanged
observer was rerun before this protocol and reproduced the canonical output
SHA-256 `8e8405087757368672736a43d858094349bea6c295029ffe4ea8a3ef15c385d8`.
Canonical output means sorted-key compact ASCII JSON with no trailing newline.
The archived SLC model lane stays historically `not_run`; this new batch is
recorded separately rather than rewriting that observation.

Native input contains only frozen control instructions, the current question
and the same ordered title/ID catalog. Do not send labels, prepared keywords,
baseline hit lists, report prose, summaries, private registries or credentials
inside the body. No external source is fetched. These are six authored,
dependent development cases over reused public metadata, not unseen tasks,
human gold, original-paper truth or measured user behavior.

Construct a local snapshot with all summaries `None`. Required local metadata
must explicitly identify public bibliography projection or unavailable values;
never invent a publisher, abstract, date of original retrieval or source truth.
Selected IDs invoke the unchanged local read and produce `missing_text`, not
an excerpt. This can be correct location while delivering zero evidence text.

Before edits, all twenty titles fit with zero omissions/truncations. The six
full native ASCII requests measured 4376, 4469, 4374, 4383, 4385 and 4378 bytes;
callbacks measured 4253, 4346, 4251, 4260, 4262 and 4255 bytes. These are offline
capacity observations, not provider acceptance.

The primary comparator is `prepared_keyword_assisted`, not whole-question
literal zero hits. Report candidate sets/counts, acceptable intersection, set
coverage and at-least-one acceptable location separately. Selecting either P5
or P8 passes SLC04 with one-half reference coverage; the keyword pair also
passes, with full coverage. Neither candidate reduction nor the pre-supplied
English keywords establishes saved human effort.

For SLC06 only explicit decline matches the no-fitting-title reference.
Provider refusal, unavailable observation and unrun case must not become
successful empty selections. Keep five positive and one no-fit denominators,
actual evaluable counts and all planned cases. Reference agreement is not
general source relevance or entailment. No human time or adoption metric is
fabricated, and assistant/reference-review effort is not costed.

## Bounded operational authority

The owner's standing permission for low-cost Qwen work, and their continuation
of the same-public-title comparison, is narrowed to this new batch:

- exact `qwen3.5-plus` at the existing pinned DashScope Beijing endpoint;
- at most six sequential requests, one per case, aggregate USD 0.10 soft stop;
- existing conservative per-request reservation USD 0.011149312, six total
  USD 0.066895872; these frozen estimates are not current vendor invoices;
- accept one in-flight request may slightly exceed the soft line;
- public questions/catalog only, no private text, retries, redirects, repair,
  fallback, recovery, extra searches, replacement model or production activation.

This does not reuse SLQ's output or consumed allowance. Parent execution binds
the exact committed code and fixture after independent review and green CI.
CLI acknowledgement records the operator action, not independent consent,
review or CI verification. Tests and implementation make no native requests.

The runner resolves only process `DASHSCOPE_API_KEY`; no automatic dotenv or
other-provider fallback. The parent operator may explicitly supply that single
existing dedicated value from local configuration without printing/persisting
it. This is not authority to upload configuration files or use another key.

## New runner, persistence and accounting

Add `academic_agent.report_evidence_source_locator_comparison_qwen` and its
dedicated test module. Default module CLI verifies identity only: no key read,
output creation or provider call. Explicit paid acknowledgement must equal the
new protocol identity. Reuse unchanged locator and dedicated native transport,
not the closed SLQ runner/global limits. Do not refactor hash-frozen old code
to simplify the new module.

Use only `outputs/report_evidence_source_locator_comparison_qwen_v1`, once.
Occupied/partial, symlink and reparse destinations are rejected without a new
directory, resume or cleanup-and-rerun. Bind committed/disk code, tests, this
protocol, existing SLC fixture/report/observer assets, actual transitive imports,
dependency lock/installed versions, configuration, six request hashes and the
expected baseline hash. CRLF-to-LF normalization is fixed and explicit.

Run the unchanged Node baseline before key lookup or output creation. Require
complete available observations and the frozen canonical hash; unavailable or
changed output stops before any paid admission. Persist identity, baseline,
experiment and operator acknowledgement before native work. Each case owns
one new single-request native ledger; an independent aggregate gate requires
sum(max(reserved, known estimate)) plus the next reservation not exceed 0.10.
Unknown usage, pending intent and failed publication cannot be refunded.

Recheck identity before and after each callback, including exceptional exits,
without losing usage. Reconcile complete callback/native bytes, exact model,
coherent usage, reserve/finish events, admitted choice, actual local read and
serialized code-owned result. Expose observed callback/reservation/HTTP-primitive
entry/response/read counts with their limits; an HTTP entry is not independent
proof of network delivery or provider exactly-once execution.

Publish complete JSON atomically and write-once after flush/fsync/close. Retain
unknown or pending spend and fixed safe failure categories, not raw provider
messages, refusal prose, exception strings, keys or arbitrary diagnostics.
Single-owner local durability does not claim power-loss/distributed safety.

## Gates, stopping and observations

First apply mechanical reconciliation, then frozen reference membership.
A wrong but valid visible ID, inappropriate explicit decline or standalone
provider refusal can pass mechanics while failing the reference. Missing text
is expected after a correct positive location, not mechanical failure.

First mechanical, reference, identity, budget, unknown-usage or publication
failure stops all following cases. Keep those cases explicitly unrun with null
metrics; the six-request ceiling does not guarantee six observations. A partial
batch cannot become a complete comparison or silently improve its denominator.

Overall success requires six mechanical/reference passes, complete case and
summary publication and no unresolved accounting. Preserve per-case and paired
candidate metrics even for disagreement, along with monotonic end-to-end timing,
its local validation/publication scope, reported tokens, estimated costs and
reservations. Do not call these isolated provider latency, an SLO, measured
human cost, semantic correctness or a benefit claim.

## Validation and later judgment

Intercept the actual HTTP primitive with fake keys while running the real
locator and native adapter. Assert exact request body and current question,
catalog equality, no keyword/label/prose leakage, actual reads and receipt
publication. Do not replace successful selector results with scripted grades.

Cover both P5/P8 choices, explicit decline versus refusal, missing text,
unknown usage, wrong visible ID, pre/post identity drift, baseline failure,
occupied/indirect output, aggregate request/budget gates, persistence faults,
safe CLI errors and unrun/null counts. Test the request/publication seams.
Reinject stop bypass, mechanical/reference conflation, refusal-as-empty success
and pre-dispatch identity or aggregate-budget bypass; unchanged tests must fail
and exact source bytes must be restored.

Run before/after full zero-provider suites, latest Ruff and narrow Pylint.
An independent read-only review and all CI checks on the exact tree precede
native use. Any later semantic inspection uses a fresh label-blinded LLM context
with honest provenance, not a fabricated human review or extra paid judge call.
It cannot overwrite frozen references or repair this batch. Publish a qualified
new result and current pointers; do not promote it to report support, private
saved-text readiness or a website feature.
